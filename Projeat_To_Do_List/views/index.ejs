<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        /* Apply Background Image to Index Page */
        body {
            background-image: url('https://img.freepik.com/premium-photo/list-icon-notebook-with-completed-todo-list-3d-render_471402-428.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
            position: relative;
        }

        /* Dark overlay for readability */
        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            z-index: -1;
        }

        h1 {
            color: #fff;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
            margin-bottom: 20px;
            font-size: 2rem;
            text-align: center;
            border-bottom: 2px solid #ffffff;
        }

        /* Form Container with Glassmorphism Effect */
        .form-container {
            background: rgba(255, 255, 255, 0.3);
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            width: 700px;
            text-align: center;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        /* Input Styling */
        .form-container input[type="text"] {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 8px;
            font-size: 1.5rem;
            outline: none;
            background: rgba(255, 255, 255, 0.8);
            transition: 0.3s;
        }

        .form-container input[type="text"]:focus {
            background: rgba(255, 255, 255, 1);
            box-shadow: 0 0 8px rgba(255, 255, 255, 0.6);
        }

        /* Beautiful Button */
        .form-container input[type="submit"] {
            background: linear-gradient(135deg, #4caf50, #45a049);
            color: #fff;
            border: none;
            padding: 12px;
            cursor: pointer;
            border-radius: 8px;
            width: 100%;
            font-size: 1.5rem;
            margin-top: 15px;
            transition: all 0.3s ease-in-out;
        }

        .form-container input[type="submit"]:hover {
            background: linear-gradient(135deg, #388e3c, #2e7d32);
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(67, 160, 71, 0.4);
        }

        /* Todo List Styling */
        .todo-list {
            background: rgba(255, 255, 255, 0.3);
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            width: 700px;
            max-height: 400px;
            overflow-y: auto;
            margin-top: 20px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .todo-item {
            background: #f9f9f9;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            transition: 0.3s;
        }

        .todo-item:hover {
            background: #ececec;
            transform: scale(1.02);
        }

        .todo-item span {
            max-height: 50px;
            overflow-y: auto;
            word-break: break-word;
            flex: 1;
            font-size: 1.5rem;
            color: #333;

        }

        /* Scrollbar */
        .todo-list::-webkit-scrollbar {
            width: 8px;
        }

        .todo-list::-webkit-scrollbar-track {
            background: #f1f1f1;
            border-radius: 10px;
        }

        .todo-list::-webkit-scrollbar-thumb {
            background: #888;
            border-radius: 10px;
        }

        .todo-list::-webkit-scrollbar-thumb:hover {
            background: #555;
        }

        .cantener {
            display: flex;
            gap: 5px;
        }

        .delete-btn,
        .update-btn {
            border: none;
            padding: 6px 12px;
            cursor: pointer;
            border-radius: 6px;
            font-size: 1rem;
            transition: 0.3s;
        }

        .delete-btn {
            background: #dc3545;
            color: white;
        }

        .delete-btn:hover {
            background: #c82333;
        }

        .update-btn {
            background: #ffc107;
            color: white;
        }

        .update-btn:hover {
            background: #e0a800;
        }

        a {
            text-decoration: none;
            color: white;
            font-weight: bold;
        }

        /* Responsive Design */
        @media (max-width: 400px) {

            .form-container,
            .todo-list {
                width: 100%;
            }
        }
    </style>
</head>

<body>

    <h1>To-Do List</h1>

    <div class="form-container">
        <form action="/insertTodo" method="post">
            <input type="text" name="todo" required placeholder="Add a new task...">
            <input type="submit" value="Add Task">
        </form>
    </div>

    <div class="todo-list">
        <% userTodo.map((user)=> { %>
            <div class="todo-item">
                <span>
                    <%= user.todo %>
                </span>
                <div class="cantener">
                    <button class="delete-btn"><a href="/DeleteData?userid=<%-user.userId%>">Delete</a></button>
                    <button class="update-btn"><a href="/EditData?userid=<%=user.userId%>">Edit</a></button>
                </div>
            </div>
            <% }) %>
    </div>

</body>

</html>