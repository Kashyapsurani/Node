<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* 🌟 Global Styles */
body {
    font-family: "Poppins", sans-serif;
    background: linear-gradient(to right, #1e3c72, #2a5298);
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    flex-direction: column;
    flex-wrap: wrap;
}

/* 🏆 Form Container */
form {
    background: white;
    padding: 40px;
    border-radius: 12px;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    width: 350px;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* Header */
h2 {
    text-align: center;
    color: #ffffff;
    text-transform: uppercase;
    margin-bottom: 20px;
}

/* 📝 Form Fields */
label {
    font-weight: bold;
    color: #333;
    margin-bottom: 5px;
    width: 100%;
    text-align: left;
}

/* Inputs */
input[type="text"],
select {
    width: 100%;
    padding: 12px;
    border-radius: 6px;
    border: 1px solid #ccc;
    font-size: 16px;
    margin-bottom: 15px;
    transition: 0.3s;
}

input[type="text"]:focus,
select:focus {
    border-color: #007bff;
    outline: none;
}

/* 🔘 Update Button */
button {
    width: 100%;
    padding: 12px;
    background: #007bff;
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-size: 16px;
    font-weight: bold;
    transition: 0.3s;
}

button:hover {
    background: #0056b3;
}

/* 📱 Responsive Design */
@media (max-width: 400px) {
    form {
        width: 90%;
    }
}

    </style>
</head>
<body>
    <!-- views/update-question.ejs -->
    <h2>Update Question</h2>
    <form action="/admin/update-question" method="POST">
        <input type="hidden" name="questionId" value="<%= question._id %>">
    
        <label for="question">Question:</label>
        <input type="text" name="question" value="<%= question.question %>" required>
    
        <label>Option 1:</label>
        <input type="text" name="options[]" value="<%= question.options[0] %>" required>
    
        <label>Option 2:</label>
        <input type="text" name="options[]" value="<%= question.options[1] %>" required>
    
        <label>Option 3:</label>
        <input type="text" name="options[]" value="<%= question.options[2] %>" required>
    
        <label>Option 4:</label>
        <input type="text" name="options[]" value="<%= question.options[3] %>" required>
    
        <label>Correct Answer:</label>
        <select name="answer">
            <option value="0" <%=question.answer==0 ? 'selected' : '' %>>Option 1</option>
            <option value="1" <%=question.answer==1 ? 'selected' : '' %>>Option 2</option>
            <option value="2" <%=question.answer==2 ? 'selected' : '' %>>Option 3</option>
            <option value="3" <%=question.answer==3 ? 'selected' : '' %>>Option 4</option>
        </select>
    
        <label>Category:</label>
        <select name="category">
            <option value="html" <%=question.category=='html' ? 'selected' : '' %>>HTML</option>
            <option value="css" <%=question.category=='css' ? 'selected' : '' %>>CSS</option>
            <option value="javascript" <%=question.category=='javascript' ? 'selected' : '' %>>JavaScript</option>
            <option value="react" <%=question.category=='react' ? 'selected' : '' %>>React</option>
        </select>
    
        <button type="submit">Update Question</button>
    </form>

</body>
</html>